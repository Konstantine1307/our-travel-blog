export default function ImagePostTitle({ children }) {
  return (
    <h4 className='text-sm text-black   tracking-tighter leading-tight  p-2 text-center md:text-left'>
      {children}
    </h4>
  );
}